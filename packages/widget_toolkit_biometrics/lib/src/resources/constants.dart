/// Default English string passed to [BiometricsService.enableBiometrics] when
/// [BiometricsSwitch.localizedReason] is omitted and the user turns biometrics on.
const String activateBiometrics = 'Activate the biometrics of your device';

/// English wording hosts can reuse when mirroring package copy for a disable
/// flow or when supplying their own toggle UI that should stay consistent with
/// the enable prompt tone.
const String deactivateBiometrics = 'Deactivate the biometrics of your device';
