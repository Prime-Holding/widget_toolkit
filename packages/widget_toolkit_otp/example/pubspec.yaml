name: otp_example
description: An example project to present how to use widget_toolkit_otp package with minimal effort.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  widget_toolkit: ^0.0.1-dev10
  widget_toolkit_otp: ^0.0.1-dev5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
